name: Daily sector snapshot
on:
  schedule:
    - cron: '0 10 * * 1-5'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/build-snapshot.mjs'
      - 'scripts/build-low-base.mjs'
      - '.github/workflows/daily-snapshot.yml'
permissions:
  contents: write
concurrency:
  group: daily-sector-snapshot
  cancel-in-progress: false
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Sync with latest main before building
        run: |
          git fetch origin main
          git reset --hard origin/main
      - name: Build T-1 to T-10 snapshot
        run: node scripts/build-snapshot.mjs
      - name: Refresh official quote fallback
        run: node scripts/build-low-base.mjs
      - name: Commit snapshot if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Keep only files generated by this workflow.
          mkdir -p /tmp/daily-snapshot
          cp public/mapping.json /tmp/daily-snapshot/mapping.json
          cp public/rank-history.json /tmp/daily-snapshot/rank-history.json
          cp public/low-base.json /tmp/daily-snapshot/low-base.json

          if git diff --quiet -- public/mapping.json public/rank-history.json public/low-base.json; then
            exit 0
          fi

          # Rebase onto the latest main and restore only the generated files.
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/daily-snapshot/mapping.json public/mapping.json
          cp /tmp/daily-snapshot/rank-history.json public/rank-history.json
          cp /tmp/daily-snapshot/low-base.json public/low-base.json

          git add public/mapping.json public/rank-history.json public/low-base.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Update daily sector rank snapshot"
          git push origin HEAD:main
