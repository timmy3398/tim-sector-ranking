import fs from 'node:fs/promises';
import {execFileSync} from 'node:child_process';
let mapping;try{mapping=JSON.parse(await fs.readFile('public/mapping.json','utf8'))}catch{const old=execFileSync('git',['show','ad4aee46620456bd4732b94bae539fd0c4f78765:public/app.js'],{encoding:'utf8'}),m=old.match(/const MEMBERS=(\{[\s\S]*?\});\nlet quotes/);if(!m)throw Error('mapping recovery failed');mapping=JSON.parse(m[1]);await fs.writeFile('public/mapping.json',JSON.stringify(mapping))}
const MEMBERS=mapping,CATEGORIES=Object.keys(MEMBERS),wanted=new Set(Object.values(MEMBERS).flat());
const n=v=>{if(v==null)return null;const s=String(v).trim();if(!s||s==='-'||s==='--')return null;const x=Number(s.replaceAll(',','').replace(/[^0-9.-]/g,''));return Number.isFinite(x)?x:null};
const norm=v=>{const s=String(v||'').replace(/[^0-9]/g,'');return s.length===7?String(Number(s.slice(0,3))+1911)+s.slice(3):s};
const ymd=d=>`${d.getUTCFullYear()}${String(d.getUTCMonth()+1).padStart(2,'0')}${String(d.getUTCDate()).padStart(2,'0')}`,slash=s=>`${s.slice(0,4)}/${s.slice(4,6)}/${s.slice(6,8)}`,sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function getJson(url){const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0','accept':'application/json,text/plain,*/*'}});if(!r.ok)throw Error(`${r.status} ${url}`);return r.json()}
function parseTwse(j){const out={};for(const t of j.tables||[]){const f=t.fields||[],ci=f.findIndex(x=>String(x).includes('證券代號')),vi=f.findIndex(x=>String(x).includes('成交金額'));if(ci<0||vi<0)continue;for(const r of t.data||[]){const c=String(r[ci]||'').trim(),v=n(r[vi]);if(wanted.has(c)&&v!=null)out[c]={value:v,source:'official'}}if(Object.keys(out).length)break}return out}
function parseTpex(j){const out={};for(const t of j.tables||[]){const f=t.fields||[],ci=f.findIndex(x=>/代號/.test(String(x))),vi=f.findIndex(x=>String(x).includes('成交金額'));if(ci<0||vi<0)continue;for(const r of t.data||[]){const c=String(r[ci]||'').trim(),v=n(r[vi]);if(wanted.has(c)&&v!=null)out[c]={value:v*1000,source:'official'}}}return out}
async function fetchDay(date){const [a,b]=await Promise.allSettled([getJson(`https://www.twse.com.tw/rwd/zh/afterTrading/MI_INDEX?date=${date}&type=ALLBUT0999&response=json`),getJson(`https://www.tpex.org.tw/www/zh-tw/afterTrading/dailyQuotes?date=${encodeURIComponent(slash(date))}&response=json`)]),vals={};if(a.status==='fulfilled')Object.assign(vals,parseTwse(a.value));if(b.status==='fulfilled')Object.assign(vals,parseTpex(b.value));return vals}
async function fallback(code,date){try{const j=await getJson(`https://www.twse.com.tw/rwd/zh/afterTrading/STOCK_DAY?date=${date.slice(0,6)}01&stockNo=${code}&response=json`);for(const r of j.data||[])if(norm(r[0])===date){const official=n(r[2]);if(official!=null)return{value:official,source:'stock-official'};const vol=n(r[1]),close=n(r[6]);if(vol!=null&&close!=null)return{value:vol*close,source:'volume-close'}}}catch{}try{const j=await getJson(`https://www.tpex.org.tw/www/zh-tw/afterTrading/tradingStock?code=${code}&date=${encodeURIComponent(slash(date))}&id=&response=json`);for(const t of j.tables||[])for(const r of t.data||[])if(norm(r[0])===date){const official=n(r[2]);if(official!=null)return{value:official*1000,source:'stock-official'};const vol=n(r[1]),close=n(r[6]);if(vol!=null&&close!=null)return{value:vol*1000*close,source:'volume-close'}}}catch{}return null}
const now=new Date(Date.now()+8*3600e3),dates=[],valuesByDate={};for(let back=1;back<=35&&dates.length<10;back++){const d=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate()-back));if([0,6].includes(d.getUTCDay()))continue;const date=ymd(d);try{const vals=await fetchDay(date);if(Object.keys(vals).length>20){dates.push(date);valuesByDate[date]=vals;console.log(`official ${date}: ${Object.keys(vals).length} mapped stocks`)}}catch(e){console.warn(`skip ${date}: ${e.message}`)}await sleep(200)}dates.sort();if(dates.length<10)throw Error(`Only ${dates.length}/10 trading days were recovered`);
for(const date of dates){const vals=valuesByDate[date],missing=[...wanted].filter(c=>!vals[c]);console.log(`${date}: fallback ${missing.length} stocks`);for(let i=0;i<missing.length;i+=8){const batch=missing.slice(i,i+8),rr=await Promise.all(batch.map(async c=>[c,await fallback(c,date)]));for(const [c,v] of rr)if(v)vals[c]=v;await sleep(150)}}
const days=[];for(const date of dates){const stock=valuesByDate[date],sectors=[];for(const name of CATEGORIES){const cs=[...new Set(MEMBERS[name]||[])];let value=0,covered=0,estimated=0;for(const c of cs){const x=stock[c];if(x?.value!=null){value+=x.value;covered++;if(x.source==='volume-close')estimated++}}sectors.push({name,value,covered,total:cs.length,estimated})}const rankable=sectors.filter(x=>x.covered===x.total).sort((a,b)=>b.value-a.value),ranks={};rankable.forEach((x,i)=>ranks[x.name]=i+1);days.push({date,ranks,complete:rankable.length,total:CATEGORIES.length,estimatedSectors:rankable.filter(x=>x.estimated).length})}
const latest=dates.at(-1),latestStocks=valuesByDate[latest],prevValues={};for(const name of CATEGORIES){const cs=[...new Set(MEMBERS[name]||[])];let value=0,covered=0;for(const c of cs){const x=latestStocks[c];if(x?.value!=null){value+=x.value;covered++}}if(covered===cs.length)prevValues[name]=value}
const unresolved=[...wanted].filter(c=>dates.some(d=>!valuesByDate[d][c]));const out={version:4,generatedAt:new Date().toISOString(),latestDate:latest,days,prevValues,unresolved,source:'TWSE/TPEx market official -> per-stock official -> volume × close fallback'};await fs.writeFile('public/rank-history.json',JSON.stringify(out));console.log(`saved ${days.length} days; latest ${latest}; latest complete ${Object.keys(prevValues).length}/${CATEGORIES.length}; unresolved stocks ${unresolved.length}`);
