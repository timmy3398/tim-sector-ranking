const TWSE_CLOSE='https://openapi.twse.com.tw/v1/exchangeReport/STOCK_DAY_ALL';
const TPEX_CLOSE='https://www.tpex.org.tw/openapi/v1/tpex_mainboard_daily_close_quotes';
const CORS={'access-control-allow-origin':'*','access-control-allow-methods':'GET,OPTIONS','access-control-allow-headers':'Content-Type'};
const n=v=>{if(v==null)return null;const s=String(v).trim();if(!s||s==='-'||s==='--')return null;const x=Number(s.replaceAll(',','').replace(/[^\d.-]/g,''));return Number.isFinite(x)?x:null};
const pick=(o,ks)=>{for(const k of ks)if(o?.[k]!=null&&o[k]!=='')return o[k];return null};
async function jf(url){const r=await fetch(url,{headers:{'user-agent':'Mozilla/5.0','accept':'application/json,text/plain,*/*'},cf:{cacheTtl:0}});if(!r.ok)throw Error(`${r.status} ${url}`);return r.json()}
function json(data,cache='public, max-age=15'){return new Response(JSON.stringify(data),{headers:{...CORS,'content-type':'application/json; charset=utf-8','cache-control':cache}})}
function normDate(v){const s=String(v||'').replace(/[^0-9]/g,'');if(s.length===8)return s;if(s.length===7)return String(Number(s.slice(0,3))+1911)+s.slice(3);return s}
function tw(){return new Date(Date.now()+8*3600e3)}
function mins(){const d=tw();return d.getUTCHours()*60+d.getUTCMinutes()}
function weekday(){const d=tw(),x=d.getUTCDay();return x>=1&&x<=5}
function marketOpen(){const m=mins();return weekday()&&m>=540&&m<=810}
function useMis(){const m=mins();return weekday()&&m>=525&&m<=1200}
function today8(){const d=tw();return `${d.getUTCFullYear()}${String(d.getUTCMonth()+1).padStart(2,'0')}${String(d.getUTCDate()).padStart(2,'0')}`}
function parseClose(o,market){const code=String(pick(o,['Code','SecuritiesCompanyCode'])||'').trim();if(!/^\d{4}$/.test(code))return null;return{code,name:String(pick(o,['Name','CompanyName','SecuritiesCompanyName'])||code).trim(),close:n(pick(o,['ClosingPrice','Close','ClosePrice'])),change:n(pick(o,['Change','ChangeAmount'])),value:n(pick(o,['TradeValue','TransactionAmount','TransactionValue','Amount'])),date:normDate(pick(o,['Date','TradeDate'])),market,source:'最近收盤'}}
async function quotes(url){const [a,b]=await Promise.allSettled([jf(TWSE_CLOSE),jf(TPEX_CLOSE)]),out={};if(a.status==='fulfilled')for(const o of a.value){const q=parseClose(o,'tse');if(q)out[q.code]=q}if(b.status==='fulfilled')for(const o of b.value){const q=parseClose(o,'otc');if(q)out[q.code]=q}let requested=[...new Set((url.searchParams.get('codes')||'').split(',').filter(x=>/^\d{4}$/.test(x)))];if(!requested.length)requested=Object.keys(out);requested=requested.filter(c=>out[c]);if(useMis()){const today=today8(),batches=[];for(let i=0;i<requested.length;i+=80)batches.push(requested.slice(i,i+80));for(let w=0;w<batches.length;w+=6){const rr=await Promise.allSettled(batches.slice(w,w+6).map(async batch=>jf('https://mis.twse.com.tw/stock/api/getStockInfo.jsp?ex_ch='+encodeURIComponent(batch.map(c=>`${out[c].market}_${c}.tw`).join('|'))+'&json=1&delay=0&_='+Date.now()+Math.random())));for(const r of rr)if(r.status==='fulfilled')for(const x of r.value.msgArray||[]){const c=String(x.c||''),d=normDate(x.d),p=n(x.z)??n(x.trade?.z)??n(x.pz),y=n(x.y),v=n(x.v);if(out[c]&&d===today&&(p!=null||v!=null))out[c]={...out[c],close:p??out[c].close,change:p!=null&&y!=null?p-y:out[c].change,value:v!=null&&p!=null?v*1000*p:out[c].value,date:d,source:marketOpen()?'盤中':'今日收盤',time:x.trade?.t||x.t||''}}}}return json({ok:true,updated:new Date().toISOString(),quotes:out},useMis()?'no-store':'public, max-age=300')}
function isoDate(d){return `${d.getUTCFullYear()}${String(d.getUTCMonth()+1).padStart(2,'0')}${String(d.getUTCDate()).padStart(2,'0')}`}
function slashDate(s){return `${s.slice(0,4)}/${s.slice(4,6)}/${s.slice(6,8)}`}
function candidateDates(){const a=[],d=tw();for(let i=1;i<=20;i++){const x=new Date(Date.UTC(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate()-i)),day=x.getUTCDay();if(day!==0&&day!==6)a.push(isoDate(x))}return a.slice(0,14)}
function parseTwseDay(j,date,want){const out={};for(const t of j.tables||[]){const f=t.fields||[],ci=f.findIndex(x=>String(x).includes('證券代號')),vi=f.findIndex(x=>String(x).includes('成交金額'));if(ci<0||vi<0)continue;for(const r of t.data||[]){const c=String(r[ci]||'').trim();if(want.has(c)){const v=n(r[vi]);if(v!=null)out[c]={date,value:v,market:'tse'}}}if(Object.keys(out).length)break}return out}
function parseTpexDay(j,date,want){const out={};for(const t of j.tables||[]){const f=t.fields||[],ci=f.findIndex(x=>/代號/.test(String(x))),vi=f.findIndex(x=>String(x).includes('成交金額'));if(ci<0||vi<0)continue;for(const r of t.data||[]){const c=String(r[ci]||'').trim();if(want.has(c)){const v=n(r[vi]);if(v!=null)out[c]={date,value:v*1000,market:'otc'}}}}return out}
async function historySnapshot(url){const codes=[...new Set((url.searchParams.get('codes')||'').split(',').filter(x=>/^\d{4}$/.test(x)))].sort(),want=new Set(codes),values=Object.fromEntries(codes.map(c=>[c,[]])),dates=candidateDates();let good=[];for(let i=0;i<dates.length&&good.length<10;i+=3){const group=dates.slice(i,i+3),rs=await Promise.all(group.map(async d=>({d,r:await Promise.allSettled([jf(`https://www.twse.com.tw/rwd/zh/afterTrading/MI_INDEX?date=${d}&type=ALLBUT0999&response=json`),jf(`https://www.tpex.org.tw/www/zh-tw/afterTrading/dailyQuotes?date=${encodeURIComponent(slashDate(d))}&response=json`)])})));for(const {d,r} of rs){let day={};if(r[0].status==='fulfilled')Object.assign(day,parseTwseDay(r[0].value,d,want));if(r[1].status==='fulfilled')Object.assign(day,parseTpexDay(r[1].value,d,want));if(Object.keys(day).length){good.push(d);for(const [c,x] of Object.entries(day))values[c].push(x)}}}good=good.sort().slice(-10);for(const c of codes)values[c]=values[c].filter(x=>good.includes(x.date)).sort((a,b)=>a.date.localeCompare(b.date));return json({ok:true,mode:'whole-market-snapshot',dates:good,covered:Object.values(values).filter(a=>a.length).length,values},'public, max-age=21600')}
async function cached(request,fn){const cache=caches.default,hit=await cache.match(request);if(hit)return hit;const r=await fn();if(r.ok)await cache.put(request,r.clone());return r}
export default{async fetch(request,env){if(request.method==='OPTIONS')return new Response(null,{headers:CORS});const u=new URL(request.url);try{if(u.pathname==='/api/quotes')return useMis()?quotes(u):cached(request,()=>quotes(u));if(u.pathname==='/api/history'||u.pathname==='/api/history-snapshot')return cached(request,()=>historySnapshot(u));return env.ASSETS.fetch(request)}catch(e){return json({ok:false,error:String(e)},'no-store')}}};